name: Docker publish (reusable)

on:
  workflow_call:
    inputs:
      version:
        type: string
        required: true
      repository-name:
        type: string
        default: 'elementsproject'
      push-latest:
        type: string
        default: 'false'

jobs:
  publish:
    name: Tag and publish ${{ matrix.target }}
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: lightningd
            tag_suffix: ''
          - target: lightningd-vls-signer
            tag_suffix: '-vls'
    steps:
    - name: Setup Docker Buildx
      uses: docker/setup-buildx-action@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Download platform digests for this target
      uses: actions/download-artifact@v8
      with:
        pattern: digest_${{ matrix.target }}_*
        path: /tmp/digests
        merge-multiple: false

    - name: Set up values
      run: |
        DIGEST_REFS=""
        for f in /tmp/digests/*/digest.txt; do
          D=$(cat "$f")
          DIGEST_REFS="$DIGEST_REFS ${{ inputs.repository-name }}/lightningd@${D}"
        done
        echo "DIGEST_REFS=$DIGEST_REFS" >> $GITHUB_ENV

        if [[ "${{ inputs.push-latest }}" == "true" ]] ||
          ([[ "${{ github.ref_type }}" == "tag" ]] && [[ ! "${{ inputs.version }}" =~ rc ]]); then
          echo "PUSHLATEST=true" >> $GITHUB_ENV
        else
          echo "PUSHLATEST=false" >> $GITHUB_ENV
        fi

    - name: Create and push real tag(s)
      run: |
        TAG_ARGS=(-t "${{ inputs.repository-name }}/lightningd:${{ inputs.version }}${{ matrix.tag_suffix }}")
        if [[ "$PUSHLATEST" == "true" ]]; then
          TAG_ARGS+=(-t "${{ inputs.repository-name }}/lightningd:latest${{ matrix.tag_suffix }}")
        fi
        docker buildx imagetools create "${TAG_ARGS[@]}" $DIGEST_REFS
